
	INCLUDE	DATA.INC

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

	CSEG
	PUBLIC	GAME
	EXTRN	LEVELINIT,MOBINIT,MOVINIT,CHARINIT,TALKINIT,DOORINIT,OBJINIT
	EXTRN	NEWFRAME,CAMDAT,GETNCHAR,SETCAMOP,LDLEVEL

GAME:	CALL	MOBINIT
	CALL	MOVINIT
	CALL	TALKINIT
	CALL	LEVELINIT
	CALL	CHARINIT
	CALL	DOORINIT
	CALL	OBJINIT

	LD	A,(CAMDAT)
	CP	-1
	JR	Z,G.LOOP
	LD	E,A
	CALL	GETNCHAR
	PUSH	HL
	POP	IX
	CALL	SETCAMOP
	LD	E,(IX+MOV.POINT+POINT.LEVEL)
	LD	D,(IX+MOV.POINT+POINT.LEVEL+1)
	CALL	LDLEVEL

G.LOOP:	CALL	NEWFRAME
	LD	A,(FINISH)
	OR	A
	JR	Z,G.LOOP

	RET

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

	PUBLIC	ENGINE
	EXTRN	THINK,ANIMATE

ENGINE:	CALL	THINK
	CALL	ANIMATE
	RET

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

	DSEG
	PUBLIC	FINISH,EDRUN

FINISH:	DB	0
EDRUN:	DB	0

